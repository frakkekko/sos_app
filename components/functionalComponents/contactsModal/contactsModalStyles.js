import { StyleSheet } from "react-native";

const contactsModalStyles = StyleSheet.create({
  centerMainView: {
    justifyContent: "flex-end",
    alignItems: "center",
  },
});

export default contactsModalStyles;
