import { StyleSheet } from "react-native";
import { Dimensions } from "react-native";

const contactListSyles = StyleSheet.create({
  mainView: { height: Dimensions.get("window").height * 0.6, width: "100%" },
});

export default contactListSyles;
