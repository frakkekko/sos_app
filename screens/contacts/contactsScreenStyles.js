import { StyleSheet } from "react-native";

const contactsScreenStyles = StyleSheet.create({
  mainView: {
    paddingTop: 40,
  },
  buttonContainer: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    height: 300,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default contactsScreenStyles;
